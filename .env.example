# URL del backend ZALVAJE (Render)
ZALVAJE_BACKEND=https://zalvaje-backend.onrender.com

# Clave del agente (debe coincidir con AGENT_KEY en Render)
AGENT_KEY=zelva-agent-secret-2025

# Opcional: abrir el navegador visible para debug (true = modo visible, false = headless)
HEADLESS=true
